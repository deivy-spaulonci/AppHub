#!/user/bin/env groovy
pipeline{
    agent none
    stages{
        stage('build'){
            agent{
                docker {image 'maven'}
            }
            steps{
                sh "maven pakage -Dmaven.test.skip=true"
            }
       }
    }
}